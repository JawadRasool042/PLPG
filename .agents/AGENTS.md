# Project Rules

- **Git Rules**: Never push code directly to the `main` branch unless the user explicitly tells you to do so.
- **Git Rules**: Create a new branch for each module you work on (there will be 7-8 modules).

### Test Credentials
- **Email**: `jawadrasool093@gmail.com`
- **Password**: `123456Jr`
